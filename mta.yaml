## Generated mta.yaml based on template version 0.4.0
## appName = risk-management
## language=nodejs; multitenant=false
## approuter=
_schema-version: '3.1'
ID: riskmanagementcap
version: 1.0.0
description: "A simple CAP project."
parameters:
  enable-parallel-deployments: true

build-parameters:
  before-all:
    - builder: custom
      commands:
        - npx cds build --production
        - npx -p @sap/cds-dk cds build --production
        - npx rimraf gen/db/src/gen/data

modules:
  # --------------------- SERVER MODULE ------------------------
  - name: riskmanagementcap-srv
  # ------------------------------------------------------------
    type: nodejs
    path: gen/srv
    parameters:
      buildpack: nodejs_buildpack
    build-parameters:
      builder: npm
      ignore:
        - 'default-*.json'
        - .env
        - '*node_modules*'
        - package-lock.json
    provides:
      - name: srv-api # required by consumers of CAP services (e.g. approuter)
        properties:
          srv-url: ${default-url}
    requires:
      - name: riskmanagementcap-auth
      - name: riskmanagementcap-db
      - name: riskmanagementcap-destination

  # -------------------- SIDECAR MODULE ------------------------
  - name: riskmanagementcap-db-deployer
  # ------------------------------------------------------------
    type: hdb
    path: gen/db
    parameters:
      buildpack: nodejs_buildpack
    build-parameters:
      ignore: ["default-env.json", ".env"] 
    requires:
      - name: riskmanagementcap-db
      - name: riskmanagementcap-auth

  # --------------------  APPROUTER ----------------------------
  - name: riskmanagementcap-approuter
  # ------------------------------------------------------------
    type: nodejs
    path: approuter
    requires:
      - name: riskmanagementcap-auth
      - name: srv-api
        group: destinations
        properties:
          forwardAuthToken: true
          strictSSL: true
          name: srv-binding
          url: "~{srv-url}"
    build-parameters:
      requires:
        - name: riskmanagementcap-app
          artifacts:
            - ./*
          target-path: resources
      ignore:
        - 'default-*.json'
        - .env
        - '*node_modules*'
        - package-lock.json
    parameters:
      memory: 256M
      disk-quota: 512M
      keep-existing-routes: true

  # ------------------- UI -------------------------------------
  - name: riskmanagementcap-app
  # ------------------------------------------------------------
    type: html5
    path: app
    build-parameters:
      supported-platforms: []

resources:
  # services extracted from CAP configuration
  # 'service-plan' can be configured via 'cds.requires.<name>.vcap.plan'
  # ------------------------------------------------------------
  - name: riskmanagementcap-auth
  # ------------------------------------------------------------ 
    type: org.cloudfoundry.managed-service
    parameters:
      service: xsuaa
      service-plan: application
      path: ./xs-security.json
      config:
        xsappname: riskmanagementcap-${org}-${space}
        tenant-mode: dedicated
        role-collections:
          - name: 'RiskManagerCAP-${space}'
            description: Manage Risks
            role-template-references:
              - $XSAPPNAME.RiskManagerCAP
          - name: 'RiskViewerCAP-${space}'
            description: View Risks
            role-template-references:
              - $XSAPPNAME.RiskViewerCAP

  # ------------------------------------------------------------            
  - name: riskmanagementcap-db
  # ------------------------------------------------------------
    type: com.sap.xs.hdi-container
    parameters:
      service: hana
      service-plan: hdi-shared

  # ------------------------------------------------------------
  - name: riskmanagementcap-destination
  # ------------------------------------------------------------
    type: org.cloudfoundry.managed-service
    parameters:
      service: destination
      service-plan: lite
